---
interface Props {
  text: string | undefined;
  size?: "xs" | "sm" | "md" | "lg";
  className?: string;
}

const { text, size = "md", className } = Astro.props;

const sizes = {
  xs: "text-xs",
  sm: "text-sm",
  md: "text-base",
  lg: "text-lg",
};

const completeClass = `
  ${sizes[size]}
  dark:text-white
  text-black
  opacity-80
  ${className}
`;
---

<p class={completeClass}>{text}</p>
